# Calm Companion controlled beta gates

Scope: controlled production verification. This record does not authorise broad public promotion, paid sale, or connection of the printable QR code.

| Gate | Acceptance evidence | Current state |
| --- | --- | --- |
| Isolated preview | `codex/commercial-redesign` deploys to its Cloudflare Pages branch URL and production remains unchanged | Passed. `605af9d` is the fixed interface candidate and was verified at `https://cd5e5666.apc-calm-companion.pages.dev/`. Later commits on the branch affect launch records or verification tooling only. The latest recorded operations snapshot is source `2d92f38` at `https://3f7ae859.apc-calm-companion.pages.dev/`; participants use the stable branch URL. Production remains on `e5d76c2` |
| Automated release | Lint, tests, production build, release verification, build verification, and `npm audit` all pass from the exact candidate in a path containing spaces | Passed for final candidate `605af9d`: 56 tests pass and the dependency audit reports 0 vulnerabilities. The export query runs against the actual D1 migration schema. Lighthouse on deployment `cd5e5666` reports 100 accessibility, 100 best practices, and 95 performance with no warning. A separate 12-check black-box audit passed every action, tool, profile, feedback-draft, install-dialog, and browser-history check with no page or console error. Tests also cover exact install visuals, APC brand tokens and fonts, empty editable examples, bounded navigation hashes, unique evidence-link names, feedback-reference copy, two-letter profile initials, label-in-name protection, explicit production and preview export targeting, spreadsheet-formula protection, the mobile fixed-navigation correction, and enlarged-text tool-label reflow |
| Feedback fails closed | Missing D1 or Turnstile configuration returns an unavailable state and stores nothing | Passed |
| Preview feedback | Preview-only D1, preview Turnstile keys, exact origin restrictions, successful insert, and reference-based removal are demonstrated | Passed on 3 September 2026 for final candidate `605af9d` through the stable branch URL. The site-wide Feedback button opened the form, the security check enabled submission, HTTP 201 returned one reference, the confirmation displayed it, and exactly one matching D1 row stored the expected `yes`, `wording`, and `1.0.0-beta.1` fields. The response did not echo the comment. The synthetic row was deleted and a follow-up query returned zero matches. GET, foreign-origin, wrong-content-type, missing-token, and immutable-hostname requests were rejected as designed |
| Production configuration | A separate production D1 database, real hostname-restricted Turnstile keys, exact production variables, migration, fail-closed runtime, and rate limit are visible before merge | Passed on 3 September 2026. `FEEDBACK_DB` is bound to the migrated `apc-calm-feedback-production` database. The managed Turnstile widget is restricted to `calm.autismpathwaysconsulting.com`; its secret is encrypted. The exact production origin, hostname, app version, and site key are active, with no preview test-mode variable. Pages is fail closed. The active zone rule blocks an IP for 10 seconds after more than five matching POST requests in 10 seconds; 10 seconds and Block were the only period and action offered by the current plan |
| App-specific policies | Calm Companion data flow, optional local-only nickname, shared-device privacy, feedback retention, and usage boundaries are present on the live APC privacy and terms pages | Passed on 3 September 2026. Policy commit `9fb4834` was fast-forwarded to `main`; cache-busted live checks confirmed the Calm Companion disclosures and free, general educational, non-emergency usage boundaries |
| Production deployment and feedback | The reviewed app is live, restrictive response headers remain active, one real Turnstile submission reaches production D1, and its optional test comment is removed | Pending gated rollout. Production remains on `e5d76c2` and does not yet reflect interface candidate `605af9d`. Endpoint headers and route checks on `calm.autismpathwaysconsulting.com` are verified, but there is no validated post-promotion production feedback insertion yet. Custom domain must not be promoted until physical-device and five-parent gates pass, then must be rechecked after promotion |
| Device and accessibility check | iPhone, Android, desktop, keyboard, and one screen-reader path pass without a material defect | Browser evidence passed on final deployment `cd5e5666`: Lighthouse reports 100 accessibility, the desktop keyboard and skip-link sequence pass, reduced-motion emulation applies near-zero transitions and animations with automatic scrolling, Edge at 390 by 844 reopens from the activated service-worker cache while an uncached external page fails, and the timer retained four elapsed seconds after a 3.2-second background interval before pausing and resetting correctly. Actions, Tools, More, and both install dialogs have no page overflow or clipped visible text at 320 and 390 pixels with 200-percent text. Physical iPhone, Android, VoiceOver, TalkBack, increased-text, reduced-motion, installation, offline-reopen, and timer-background checks remain pending |
| Five-parent beta | All safety criteria and at least 4 of 5 independent-use criteria in `operations/beta-test-plan.md` pass | Pending |
| Beta decision | One documented Continue, Revise once, or Stop decision is made from the five-parent evidence | Pending. Automated and live-preview verification support beginning the controlled beta. Public production, broad promotion, and paid release remain blocked by the physical-device, assistive-technology, five-parent, printable, seller, and access-control evidence gaps |

## Release rule

The app may move to a limited organic public release only when every gate above passes. Paid printable sales and mass promotion remain separate later decisions.
