# Calm Companion controlled beta gates

Scope: controlled production verification. This record does not authorise broad public promotion, paid sale, or connection of the printable QR code.

| Gate | Acceptance evidence | Current state |
| --- | --- | --- |
| Isolated preview | `codex/commercial-redesign` deploys to its Cloudflare Pages branch URL and production remains unchanged | Passed on 3 September 2026 for `f327df6`: the branch URL serves the new fifth Quick Check tool while production remains on its earlier interface. The preview returns 200 with the expected CSP, HSTS, framing, permissions, referrer, and content-type protections; `/api/feedback` rejects GET with 405 and Function security headers |
| Automated release | Lint, tests, production build, release verification, build verification, and `npm audit` all pass from the exact candidate in a path containing spaces | Passed for deployed interface candidate `c7526ea` and the local launch candidate: 56 tests pass and the dependency audit reports 0 vulnerabilities. The export query runs against the actual D1 migration schema. Private production and controlled-beta exports succeeded with one production row and two existing preview test rows. Lighthouse on the exact interface deployment reports 100 accessibility, 100 best practices, and 94 performance with no automated accessibility failure. Tests also cover exact install visuals, APC brand tokens and fonts, empty editable examples, bounded navigation hashes, unique evidence-link names, feedback-reference copy, two-letter profile initials, label-in-name protection, explicit production and preview export targeting, spreadsheet-formula protection, the mobile fixed-navigation correction, and enlarged-text tool-label reflow |
| Feedback fails closed | Missing D1 or Turnstile configuration returns an unavailable state and stores nothing | Passed |
| Preview feedback | Preview-only D1, preview Turnstile keys, exact origin restrictions, successful insert, and reference-based comment removal are demonstrated | Passed on 3 September 2026. Preview has the exact branch hostname and origin, explicit test mode, an encrypted secret, the official preview site key, and `FEEDBACK_DB` bound to `apc-calm-feedback-preview`. A fresh non-personal submission from the candidate increased the table from one row to two and stored `yes`, `wording`, and `1.0.0-beta.1`. Its optional comment was then removed and its review state set to `removed`. The synthetic local nickname was also removed from the browser |
| Production configuration | A separate production D1 database, real hostname-restricted Turnstile keys, exact production variables, migration, fail-closed runtime, and rate limit are visible before merge | Passed on 3 September 2026. `FEEDBACK_DB` is bound to the migrated `apc-calm-feedback-production` database. The managed Turnstile widget is restricted to `calm.autismpathwaysconsulting.com`; its secret is encrypted. The exact production origin, hostname, app version, and site key are active, with no preview test-mode variable. Pages is fail closed. The active zone rule blocks an IP for 10 seconds after more than five matching POST requests in 10 seconds; 10 seconds and Block were the only period and action offered by the current plan |
| App-specific policies | Calm Companion data flow, optional local-only nickname, shared-device privacy, feedback retention, and usage boundaries are present on the live APC privacy and terms pages | Passed on 3 September 2026. Policy commit `9fb4834` was fast-forwarded to `main`; cache-busted live checks confirmed the Calm Companion disclosures and free, general educational, non-emergency usage boundaries |
| Production deployment and feedback | The reviewed app is live, restrictive response headers remain active, one real Turnstile submission reaches production D1, and its optional test comment is removed | Ready for final rollout. Production remains on `e5d76c2`. Review candidate `c7526ea` is published and active in Cloudflare deployment `969671c2`. The exact deployment shows `JH`, the cross-view Feedback button, the visual iPhone and Android installation guide, correct dialog focus return, no 390-pixel horizontal overflow, correct label-in-name behaviour, and no browser errors. Production feedback and its cleaned test row remain verified, but the custom domain must be rechecked after the candidate is promoted |
| Device and accessibility check | iPhone, Android, desktop, keyboard, and one screen-reader path pass without a material defect | Browser evidence passed: Lighthouse reports 100 accessibility, the desktop keyboard and skip-link sequence pass, reduced-motion emulation applies near-zero transitions and animations with automatic scrolling, Edge at 390 by 844 reopens from the activated service-worker cache while an uncached external page fails, and the timer retained three elapsed seconds after 3.2 seconds in a background tab before pausing and resetting correctly. A local 320-pixel, 200-percent text check found and corrected a Tools-menu overflow; the corrected Actions, Tools, and More views have no page overflow or clipped text. Physical iPhone, Android, VoiceOver, TalkBack, increased-text, reduced-motion, installation, offline-reopen, and timer-background checks remain pending |
| Five-parent beta | All safety criteria and at least 4 of 5 independent-use criteria in `operations/beta-test-plan.md` pass | Pending |
| Beta decision | One documented stop, revise once, or limited-release decision is made from the evidence | Revise once. Controlled technical production verification is complete, but broad promotion and paid release remain blocked by the physical-device, accessibility, five-parent, printable, seller, and access-control evidence gaps |

## Release rule

The app may move to a limited organic public release only when every gate above passes. Paid printable sales and mass promotion remain separate later decisions.
